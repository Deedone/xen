.test-jobs-common:
  stage: test
  image: xentroops/xen_rel
  rules:
  - when: on_success

.arm64-xtf-test-needs: &arm64-xtf-test-needs
  - xen-atfe-arm64
  - linux-6.6.86-image-export
  - alpine-3.18-xen-tools-export

.qemu-arm64:
  extends: .test-jobs-common
  variables:
    LOGFILE: qemu-smoke-arm64.log
  artifacts:
    paths:
      - '*.serial'
      - '*.log'
      - 'coverage_data'
    when: always
  tags:
    - aws-arm64

.minerva-arm64:
  extends: .qemu-arm64
  when: on_success
  # Auto-running runtime producers: a single qemu boot failure should not
  # redden the pipeline. A missing/failed producer just yields less
  # runtime evidence; the corpus tolerates partial input and the
  # empty-corpus gate covers the all-failed case.
  allow_failure: true
  variables:
    # Identity for the corpus join key. config_sha256 is derived from
    # xen-config, the expanded config the instrumented build exports and
    # which arrives here via needs: xen-atfe-arm64-minerva. The static
    # job consumes the SAME xen-config, so the two sides match by
    # construction. git_sha defaults to the CI commit.
    MINERVA_GIT_SHA: "$CI_COMMIT_SHA"
    MINERVA_TARGET_ARCH: "arm64"
    MINERVA_CONFIG_NAME: "rel_arm64_defconfig+debug-info"
    MINERVA_CONFIG_FILE: "xen-config"
    MINERVA_RUNTIME_LOG_DIR: "$CI_PROJECT_DIR"
  # Pure runtime: the inherited script: runs the qemu workload and
  # tees its console output into ${LOGFILE}. The after_script packages
  # the log into a runtime artifact (parsing it standalone) without
  # any static analysis. The static-analysis tree is produced
  # independently by minerva-static-analysis; the corpus job joins
  # them later.
  after_script:
    - bash automation/renesas-scripts/minerva/package_artifacts.sh
        runtime . runtime-artifacts "$CI_JOB_NAME"
  # Boot the instrumented Xen (CONFIG_MINERVA_ANALYSIS=y) ONLY. The plain
  # xen-atfe-arm64 build (pulled by *arm64-xtf-test-needs) is excluded:
  # both builds emit binaries/xen, so depending on both overwrites the
  # instrumented binary with the uninstrumented one and the runtime
  # emits no allocation traces. List the image/rootfs deps explicitly.
  needs:
    - linux-6.6.86-image-export
    - alpine-3.18-xen-tools-export
    - xen-atfe-arm64-minerva
  artifacts:
    paths:
      - runtime-artifacts/
      - '*.log'
      - '*.serial'
    when: always

# Test jobs

# XTF is run in dom0

qemu-xtf-tests:
  extends: .qemu-arm64
  parallel:
    matrix:
      - TEST:
        - "arm64 mmu64le hyp-xen-version"
        # [XenValTestJob~~1->XenValTestCode~arm64_sysctl_physinfo_host~1]
        - "arm64 mmu64le hyp-sysctl-physinfo"
        # [XenValTestJob~~1->XenValTestCode~arm64_domctl_cacheflush~1]
        - "arm64 mmu64le hyp-domctl-cacheflush"
        # [XenValTestJob~~1->XenValTestCode~domctl_destroy_domain_using_domctl~1]
        - "arm64 mmu64le hyp-domctl-destroydomain"
        - "arm64 mmu64le hyp-domctl-pausedomain"
        # [XenValTestJob~~1->XenValTestCode~arm64_domctl_set_paging_mempool_size~1]
        - "arm64 mmu64le hyp-domctl-set-paging-mempool-size"
        - "arm64 mmu64le hyp-domctl-unpausedomain"
        # [XenValTestJob~~1->XenValTestCode~sysctl_get_vmlist_using_sysctl~1]
        - "arm64 mmu64le hyp-sysctl-getdomaininfolist"
        # [XenValTestJob~~1->XenValTestCode~arm64_domctl_getdomaininfo_domain~1]
        - "arm64 mmu64le hyp-domctl-getdomaininfo"
        # [XenValTestJob~~1->XenValTestCode~arm64_domctl_createdomain~1]
        - "arm64 mmu64le hyp-domctl-createdomain"
        # [XenValTestJob~~1->XenValTestCode~arm64_domctl_get_paging_mempool_size~1]
        - "arm64 mmu64le hyp-domctl-get-paging-mempool-size"
        # [XenValTestJob~~1->XenValTestCode~domctl_max_mem~1]
        - "arm64 mmu64le hyp-domctl-max-mem"
        # [XenValTestJob~arm64_use_rtds_sched_if_selected_via_xen_cmd_line~1]
        - "arm64 mmu64le hyp-sysctl-sched-rtds-cmdline"
        # [XenValTestJob~arm64_use_rtds_sched_if_set_in_boot_time_cpupool~1]
        - "arm64 mmu64le hyp-sysctl-sched-rtds-boot-cpupool"
        # [XenValTestJob~arm64_use_rtds_sched_if_set_via_hypercall~1]
        - "arm64 mmu64le hyp-sysctl-sched-rtds-create-cpupool"
        - "arm64 mmu64le hyp-sysctl-sched-rtds-edf"
        - "arm64 mmu64le hyp-sysctl-sched-rtds-extratime"
        # [XenValTestJob~~1->XenValTestCode~rtds_sched_configure_params_per_vcpu~1]
        - "arm64 mmu64le hyp-domctl-sched-rtds-params"
        # [XenValTestJob~~1->XenValTestCode~rtds_sched_tickle_pcpus~1]
        - "arm64 mmu64le hyp-sysctl-sched-rtds-tickle"
      # Extra domain-wide variant for the same test
      - TEST:
          # [XenValTestJob~~1->XenValTestCode~rtds_sched_configure_params_per_domain~1]
          - "arm64 mmu64le hyp-domctl-sched-rtds-params"
        EXTRA_XTF_MAKE_VARS:
          - "EXTRA_FLAGS=-DTEST_DOMAIN_WIDE"
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh ${TEST} 2>&1 | tee ${LOGFILE}
  needs:
    - *arm64-xtf-test-needs

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-xen-version:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-xen-version 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-sysctl-physinfo:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-sysctl-physinfo 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-cacheflush:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-cacheflush 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-destroydomain:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-destroydomain 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-pausedomain:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-pausedomain 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-set-paging-mempool-size:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-set-paging-mempool-size 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-unpausedomain:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-unpausedomain 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-sysctl-getdomaininfolist:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-sysctl-getdomaininfolist 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-getdomaininfo:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-getdomaininfo 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-createdomain:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-createdomain 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-get-paging-mempool-size:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-get-paging-mempool-size 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-debug-hyp-domctl-max-mem:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-domctl-max-mem 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-hyp-sysctl-sched-rtds-cmdline:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-sysctl-sched-rtds-cmdline 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-hyp-sysctl-sched-rtds-edf:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-sysctl-sched-rtds-edf 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-hyp-sysctl-sched-rtds-extratime:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-sysctl-sched-rtds-extratime 2>&1 | tee ${LOGFILE}

minerva-qemu-xtf-dom0-arm64-gcc-hyp-xen-version:
  extends: .minerva-arm64
  script:
    - ./automation/renesas-scripts/qemu-xtf.sh arm64 mmu64le hyp-xen-version 2>&1 | tee ${LOGFILE}
